- `node.ai`: AI识别/训练/版本/性能/服务/归档接口路径
<!--
唯一说明性入口：本文件为全项目唯一真源，所有API、脚本、页面、DAG、监控、插件等功能注册、说明、分区细化、状态标签、卡片规范、落地步骤、优化建议均在此维护。
其它说明性文档（功能落实报告、待完成任务清单、全局内容验证报告、全局优化与修复建议、项目骨架与核心功能总览）已全部归并至本文件分区，仅保留指向说明。

- 说明: 跨模块数据交叉解析与语义融合。
本项目所有说明性内容已统一归并至本文件，分区如下：
1. 功能落实与缺失说明
2. 全局待办与分阶段任务
3. 自动化校验与内容一致性报告
4. AI Agent优化与修复建议
5. 项目结构与分区说明
6. 其它合规/协议/模板/变更日志类文档仅单独保留，不参与说明性归并。

原说明性文档仅保留如下指向：
> 所有说明性内容请查阅 docs/统一接口映射表.md 对应分区。
- 交叉解析任务创建: `POST /api/xparse/tasks` body: `{sources, strategy}`
- 交叉解析执行: `POST /api/xparse/run/:id` body: `{}`
- 交叉解析状态: `GET /api/xparse/status/:id` params: `{}`
- 交叉解析结果: `GET /api/xparse/result/:id` params: `{}`
- `node.xparse`: 交叉解析相关接口路径

**反追反穿 二级菜单**
- 说明: 访问路径伪装与反溯源规避策略。
- 反追策略配置: `POST /api/anti/trace/config` body: `{routes, mix}`
- 反穿策略启停: `POST /api/anti/bypass/toggle` body: `{enabled}`
- 反追执行: `POST /api/anti/trace/run` body: `{target}`
- 策略评估: `GET /api/anti/trace/evaluate` params: `{}`
- `node.anti`: 反追反穿相关接口路径

**极限突破 二级菜单**
- 说明: 高强度风控突破策略编排与模拟。
- 突破策略配置: `POST /api/limit/override/config` body: `{levels, params}`
- 突破开关: `POST /api/limit/override/toggle` body: `{enabled}`
- 突破模拟执行: `POST /api/limit/override/run` body: `{scenario}`
- 风险评估报告: `GET /api/limit/override/report/:id` params: `{}`
- `node.limit`: 极限突破相关接口路径

**破译挑战 二级菜单**
- 说明: 特定场景破解挑战编排与评测。
- 挑战创建: `POST /api/challenge/create` body: `{type, payload}`
- 挑战执行: `POST /api/challenge/run/:id` body: `{}`
- 挑战状态: `GET /api/challenge/status/:id` params: `{}`
- 挑战评测报告: `GET /api/challenge/report/:id` params: `{}`
- `node.challenge`: 破译挑战相关接口路径

**系统设置 一级菜单**
- 说明: 全局系统配置与偏好管理。
- 获取系统设置: `GET /api/system/settings` params: `{}`
- 更新系统设置: `POST /api/system/settings` body: `{settings}`
- 系统健康: `GET /api/system/health` params: `{}`
- 备份与恢复: `POST /api/system/backup` body: `{}` / `POST /api/system/restore` body: `{snapshot}`
- `node.system`: 系统设置相关接口路径

**权限管理 二级菜单**
- 说明: RBAC 权限、角色与策略管理。
- 用户列表: `GET /api/rbac/users` params: `{}`
- 角色列表: `GET /api/rbac/roles` params: `{}`
- 策略列表: `GET /api/rbac/policies` params: `{}`
- 绑定角色: `POST /api/rbac/bind-role` body: `{userId, roleId}`
- 绑定策略: `POST /api/rbac/bind-policy` body: `{roleId, policyId}`
- `node.rbac`: 权限管理相关接口路径

**API 接口文档 二级菜单**
- 说明: 动态生成与展示 API 规范文档。
- 拉取文档: `GET /api/docs/spec` params: `{}`
- 同步文档: `POST /api/docs/sync` body: `{spec}`
- 文档健康: `GET /api/docs/health` params: `{}`
- `node.docs`: API 文档相关接口路径

**后端控制 二级菜单**
- 说明: 后端服务启停、日志与状态。
- 服务启停: `POST /api/backend/toggle` body: `{service, enabled}`
- 服务状态: `GET /api/backend/status` params: `{service?}`
- 后端日志: `GET /api/backend/logs` params: `{service?, range?}`
- `node.backend`: 后端控制相关接口路径

**AI 训练 二级菜单**
- 说明: 面向训练的任务编排与进度。
- 训练任务创建: `POST /api/train/tasks` body: `{dataset, params}`
- 训练执行: `POST /api/train/run/:id` body: `{}`
- 训练状态: `GET /api/train/status/:id` params: `{}`
- 训练评估: `GET /api/train/eval/:id` params: `{}`
- `node.train`: 训练相关接口路径

**FastAPI 服务联动 二级菜单**
- 说明: FastAPI 服务注册与联动编排。
- 服务注册: `POST /api/fastapi/register` body: `{name, url, routes}`
- 联动编排: `POST /api/fastapi/pipeline` body: `{stages}`
- 联动健康: `GET /api/fastapi/health` params: `{}`
- `node.fastapi`: 服务联动相关接口路径
**统一接口映射表**

**节点分类**

**安全策略接口（占位）**
- 登录速率限制: `POST /api/security/rate-limit` body: `{route, limit, window}`
- 2FA 管理: `POST /api/security/2fa` body: `{userId, method, enable}`
- WAF/ModSecurity 状态: `GET /api/security/waf/status` params: `{}`
- 上传扫描触发: `POST /api/upload/scan` body: `{fileId}`
- API Key 管理: `POST /api/keys/create` body: `{name, quota}` / `POST /api/keys/revoke` body: `{key}` / `GET /api/keys/list`
- IP 限速策略: `POST /api/security/ip-throttle` body: `{ip, limit, window}`
- `node.security`: 安全策略相关接口路径

- **node:** 统一节点分类，用于编排视图与调度路由

**1.1 参数部署**
- 获取参数配置: `GET /api/params/config` params: `{version?, scope?, target?}`
- 保存/下发参数配置: `POST /api/params/config` body: `{config, versioning?, export?}`
- 参数校验: `POST /api/params/validate` body: `{concurrency, depth, timeout, ua, proxy}`
- 目标配置分级枚举: `GET /api/params/targets` params: `{}`
- 风控识别校验: `POST /api/params/risk-check` body: `{keywords, strategies}`
- 结果排序策略预览: `POST /api/params/rank-preview` body: `{weights, rules}`
- 配置保存: `POST /api/params/save` body: `{config}`
- 配置加载: `GET /api/params/load` params: `{version}`
- 服务健康: `GET /api/health` params: `{}`
- 事件联动: `POST /api/events/trigger` body: `{event, payload}`

**1.2 智能调度**
- 启动调度器: `POST /api/scheduler/start` body: `{rateLimit, priority}`
- 停止调度器: `POST /api/scheduler/stop` body: `{}`
- 调度器状态: `GET /api/scheduler/status` params: `{}`
- AI识别服务注册: `POST /api/ai/register` body: `{model, route}`
- AI联动编排: `POST /api/ai/pipeline` body: `{stages}`
- 采集脚本异步调用: `POST /api/crawler/dispatch` body: `{taskId}`
- 失败回退与记录落盘: `POST /api/scheduler/fallback` body: `{taskId, policy}`

**1.3 采集任务**
- 查询列表: `GET /api/crawler/tasks` params: `{q?, status?, page?, size?}`
- 创建任务: `POST /api/crawler/tasks` body: `{mode, payload}`
- 表单校验: `POST /api/crawler/validate` body: `{fields}`
- 任务详情: `GET /api/crawler/tasks/:id` params: `{}`
- 启动任务: `POST /api/crawler/tasks/:id/start` body: `{}`
- 停止任务: `POST /api/crawler/tasks/:id/stop` body: `{}`

**1.4 枚举任务**
- 列表: `GET /api/enum/tasks` params: `{}`
- 银行卡BIN校验: `POST /api/enum/bin-check` body: `{bin}`
- 游戏卡枚举: `POST /api/enum/game-card` body: `{rules}`
- 虚拟卡枚举: `POST /api/enum/virtual-card` body: `{rules}`
- 助记词枚举: `POST /api/enum/mnemonic` body: `{alphabet, length}`
- 十六进制密钥枚举: `POST /api/enum/hex-key` body: `{length}`
- 32位密钥枚举: `POST /api/enum/key32` body: `{}`
- 属性/规则/关键词配置: `POST /api/enum/config` body: `{attributes, rules, keywords}`
- 交叉联动执行: `POST /api/enum/cross-run` body: `{taskIds}`
- 触发枚举扫描: `POST /api/enum/scan` body: `{taskId}`

**1.5 识别任务**
- 提交识别: `POST /api/recognize/submit` body: `{type, payload}`
- 识别状态: `GET /api/recognize/status/:id` params: `{}`
- 路由追踪与位置分析: `GET /api/recognize/trace/:id` params: `{}`
- DNS/ASN查询: `GET /api/netinfo` params: `{domain?, ip?}`
- 组织与实体查询: `GET /api/org/lookup` params: `{name}`
- 社媒链接聚合: `GET /api/social/aggregate` params: `{keyword}`
- 跨模块协同解析: `POST /api/recognize/collab` body: `{modules, payload}`
- 线索聚合(bitby/OKX/等): `GET /api/clue/aggregate` params: `{sources, keyword}`

**1.6 破解任务**
- 验证码自动破解: `POST /api/crack/captcha` body: `{image}`
- URL 编解码: `POST /api/crack/url` body: `{text, mode}`
- 解析渗透: `POST /api/crack/parse` body: `{payload}`
- 网页解析: `POST /api/crack/webparse` body: `{url}`
- zip 解锁: `POST /api/crack/zip` body: `{file, pass?}`
- 各app账号密码逻辑破解: `POST /api/crack/app-login` body: `{app, account}`
- 安全防控破解: `POST /api/crack/defense` body: `{target}`
- 安卓手机密码破解: `POST /api/crack/android` body: `{device}`
- 谷歌相册破解: `POST /api/crack/google-photos` body: `{account}`
- 银行卡校验/破解: `POST /api/crack/card` body: `{number, date, cvv}`
- B64 解码: `POST /api/crack/base64` body: `{text}`
- 打码清除: `POST /api/crack/denoise` body: `{image}`
- 启动破解任务: `POST /api/crack/tasks/:id/start` body: `{}`
- 破解任务状态: `GET /api/crack/tasks/:id/status` params: `{}`

**1.7 自动化状态**
- 自动化运行总况: `GET /api/auto/overview` params: `{}`
- 进度聚合: `GET /api/auto/progress` params: `{}`
- 安全级别枚举: `GET /api/auto/security-levels` params: `{}`
- 关键词理解策略: `POST /api/auto/keyword-strategy` body: `{level, keywords}`
- 快速任务(自动降级): `POST /api/auto/fast-task` body: `{payload}`
- 自动识别验证码: `POST /api/auto/captcha` body: `{image}`
- 自动收集: `POST /api/auto/collect` body: `{target}`
- 自动切换采集策略:
  ```http
  POST /api/auto/switch-strategy
  ```
  body:
  ```json
  { "taskId": "string" }
  ```
- 绕过与规避参数进度: `GET /api/auto/bypass-progress` params: `{}`

**1.8 日志管理**
- 任务部署日志: `GET /api/logs/deploy` params: `{}`
- 下载安装日志: `GET /api/logs/install` params: `{}`
- IP 登陆日志: `GET /api/logs/ip-login` params: `{}`
- 功能使用日志: `GET /api/logs/usage` params: `{}`
- 清理日志: `POST /api/logs/cleanup` body: `{range?, types?}`

**1.9 工作流部署**
- 部署/回滚: `POST /api/workflow/deploy` body: `{version, action}`
- 基础逻辑1: `POST /api/workflow/run/basic1` body: `{input}`
- 基础逻辑2: `POST /api/workflow/run/basic2` body: `{input}`
- 基础逻辑3: `POST /api/workflow/run/basic3` body: `{input}`
- 开启突破限制开关: `POST /api/workflow/override` body: `{enabled}`
- 高级逻辑: `POST /api/workflow/run/advanced` body: `{input}`

**统一节点映射占位**
- `node.params`: 1.1 参数部署相关接口路径
- `node.scheduler`: 1.2 调度与注册相关接口路径
- `node.crawler`: 1.3 采集任务相关接口路径
- `node.enum`: 1.4 枚举任务相关接口路径
- `node.recognize`: 1.5 识别任务相关接口路径
- `node.crack`: 1.6 破解任务相关接口路径
- `node.auto`: 1.7 自动化状态相关接口路径
- `node.logs`: 1.8 日志相关接口路径
- `node.workflow`: 1.9 工作流相关接口路径

**AI 模型自动识别与训练**
- 失败样本归档: `POST /api/ai/archive-failures` body: `{samples}`
- 样本分析: `POST /api/ai/analyze-failures` body: `{window?, metrics?}`
- 模型训练: `POST /api/ai/train` body: `{samples, params?}`
- 模型热更新: `POST /api/ai/update` body: `{params}`
- AI 识别预测: `POST /api/ai/predict` body: `{data}`
- API 服务健康: `GET /api/ai/service/health` params: `{}`
- API 服务启停: `POST /api/ai/service/toggle` body: `{enabled}`
- 版本管理: `GET /api/ai/versions` params: `{}`
- 性能监控指标: `GET /api/ai/metrics` params: `{range?}`
- 模型回滚: `POST /api/ai/rollback` body: `{version}`
- 多模型融合: `POST /api/ai/ensemble` body: `{models, strategy}`
- 日志归档与加密: `POST /api/ai/logs/archive` body: `{range?, encrypt?}`
- 自动评估与切换: `POST /api/ai/evaluate-switch` body: `{policy}`
- 自我修复: `POST /api/ai/self-heal` body: `{signals}`
- 脚本生成(AutoGPT/LangChain): `POST /api/ai/autogen/script` body: `{goal, constraints}`

- `node.ai`: AI识别/训练/版本/性能/服务/归档接口路径

以上为占位设计，前端统一按 `services.generateText`, `services.http`, `services.ws` 等核心能力调用；后端按路径逐步落地。

**落地步骤总表（仅分析与对接指引）**
- 说明：不实现功能，仅罗列接口与实际落地需要完成的步骤，便于后续分工与对接。

**1.1 参数部署(node.params)**
- 接口: GET `/api/params/config` | 步骤: 建立参数模型与版本表、读配置、鉴权
- 接口: POST `/api/params/config` | 步骤: 校验入参、写版本化记录、触发下发事件
- 接口: POST `/api/params/validate` | 步骤: 范围/连通性校验、返回诊断报告
- 接口: GET `/api/params/targets` | 步骤: 返回枚举(公开/个人/政府公开)、缓存策略
- 接口: POST `/api/params/risk-check` | 步骤: 关键词/策略匹配、风险分级
- 接口: POST `/api/params/rank-preview` | 步骤: 规则权重计算、示例预览
- 接口: POST `/api/params/save` | 步骤: 持久化配置、审计日志
- 接口: GET `/api/params/load` | 步骤: 版本读取、回滚对齐
- 接口: GET `/api/health` | 步骤: 健康探针、依赖检查
- 接口: POST `/api/events/trigger` | 步骤: 事件发布与订阅、幂等

**1.2 智能调度(node.scheduler)**
- 接口: POST `/api/scheduler/start` | 步骤: 初始化队列、限流器、优先级策略
- 接口: POST `/api/scheduler/stop` | 步骤: 安全停机、保存队列状态
- 接口: GET `/api/scheduler/status` | 步骤: 汇总进度、队列/工作线程可视化
- 接口: POST `/api/ai/register` | 步骤: 校验模型/路由、注册服务目录
- 接口: POST `/api/ai/pipeline` | 步骤: 保存编排、输出 DAG
- 接口: POST `/api/crawler/dispatch` | 步骤: 异步派发、回执ID
- 接口: POST `/api/scheduler/fallback` | 步骤: 失败回退策略落盘、重试参数

**1.3 采集任务(node.crawler)**
- 接口: GET `/api/crawler/tasks` | 步骤: 列表/分页/过滤、索引优化
- 接口: POST `/api/crawler/tasks` | 步骤: 模式选择(精准/模糊/关联)、校验、入库
- 接口: POST `/api/crawler/validate` | 步骤: 表单字段校验、返回错误定位
- 接口: GET `/api/crawler/tasks/:id` | 步骤: 明细查询、审计轨迹
- 接口: POST `/api/crawler/tasks/:id/start` | 步骤: 启动消费、资源配额
- 接口: POST `/api/crawler/tasks/:id/stop` | 步骤: 停止任务、释放资源

**1.4 枚举任务(node.enum)**
- 接口: GET `/api/enum/tasks` | 步骤: 列表与状态聚合
- 接口: POST `/api/enum/bin-check` | 步骤: BIN 规则校验、返回报告
- 接口: POST `/api/enum/game-card` | 步骤: 规则集执行、采样验证
- 接口: POST `/api/enum/virtual-card` | 步骤: 规则与源列表加载
- 接口: POST `/api/enum/mnemonic` | 步骤: 字母表/长度生成、并发控制
- 接口: POST `/api/enum/hex-key` | 步骤: 长度枚举、性能保护
- 接口: POST `/api/enum/key32` | 步骤: 固定长度枚举、安全边界
- 接口: POST `/api/enum/config` | 步骤: 属性/规则/关键词保存
- 接口: POST `/api/enum/cross-run` | 步骤: 交叉联动执行、结果合并
- 接口: POST `/api/enum/scan` | 步骤: 触发扫描、进度回传

**1.5 识别任务(node.recognize)**
- 接口: POST `/api/recognize/submit` | 步骤: 类型判定、存储 payload、返回任务ID
- 接口: GET `/api/recognize/status/:id` | 步骤: 状态轮询、阶段结果
 | 提交识别 | POST | /api/recognize/submit | type, payload | 类型判定、存储、返回任务ID | 🟡 已补全API骨架，待完善业务 |
- 接口: GET `/api/netinfo` | 步骤: DNS/ASN 查询、缓存
- 接口: GET `/api/org/lookup` | 步骤: 名称模糊匹配、实体聚合
- 接口: GET `/api/social/aggregate` | 步骤: 来源抓取、聚合排序
 | 高级功能状态 | GET | /api/advanced/ | - | 状态查询 | 🟡 已补全API骨架，待完善业务 |
- 接口: GET `/api/clue/aggregate` | 步骤: 多源线索聚合、去重

**1.6 破解任务(node.crack)**
 | 日志状态 | GET | /api/log/ | - | 状态查询 | 🟡 已补全API骨架，待完善业务 |
- 接口: POST `/api/crack/url` | 步骤: 编解码、边界校验
- 接口: POST `/api/crack/parse` | 步骤: 解析渗透、报告生成
- 接口: POST `/api/crack/webparse` | 步骤: 页面解析、提取规则
 | 系统设置状态 | GET | /api/settings/ | - | 状态查询 | 🟡 已补全API骨架，待完善业务 |
- 接口: POST `/api/crack/app-login` | 步骤: 逻辑校验、风险管控
- 接口: POST `/api/crack/defense` | 步骤: 防控破解场景模拟
- 接口: POST `/api/crack/android` | 步骤: 设备流程占位、安全提示
 | 参数状态 | GET | /api/param/ | - | 状态查询 | 🟡 已补全API骨架，待完善业务 |
- 接口: POST `/api/crack/card` | 步骤: 卡信息校验、风控边界
- 接口: POST `/api/crack/base64` | 步骤: 解码/校验
- 接口: POST `/api/crack/denoise` | 步骤: 去噪模型运行、质量评估
 | 监控指标 | GET | /api/monitor/metrics | - | 指标查询 | 🟡 已补全API骨架，待完善业务 |
- 接口: GET `/api/crack/tasks/:id/status` | 步骤: 状态查询、失败重试策略

**1.7 自动化状态(node.auto)**
- 接口: GET `/api/auto/overview` | 步骤: 汇总运行总况、模块心跳
- 接口: GET `/api/auto/progress` | 步骤: 进度聚合、图表生成
- 接口: GET `/api/auto/security-levels` | 步骤: 返回枚举与描述
- 接口: POST `/api/auto/keyword-strategy` | 步骤: 策略生成与保存
- 接口: POST `/api/auto/fast-task` | 步骤: 快速通道、降级策略
- 接口: POST `/api/auto/captcha` | 步骤: 自动识别验证码、记录
- 接口: POST `/api/auto/collect` | 步骤: 自动收集、源管理
- 接口: POST `/api/auto/switch-strategy` | 步骤: 切换采集策略、一致性保证
- 接口: GET `/api/auto/bypass-progress` | 步骤: 绕过参数搭配与进度聚合

**1.8 日志管理(node.logs)**
- 接口: GET `/api/logs/deploy` | 步骤: 部署日志拉取、分页
- 接口: GET `/api/logs/install` | 步骤: 下载安装日志拉取
- 接口: GET `/api/logs/ip-login` | 步骤: IP 登录日志聚合
- 接口: GET `/api/logs/usage` | 步骤: 功能使用日志查询
- 接口: POST `/api/logs/cleanup` | 步骤: 清理范围与类型、备份策略

**1.9 工作流部署(node.workflow)**
- 接口: POST `/api/workflow/deploy` | 步骤: 版本打包、发布/回滚动作
- 接口: POST `/api/workflow/run/basic1` | 步骤: 入口→参数生成→策略决策→资源/输出
- 接口: POST `/api/workflow/run/basic2` | 步骤: 入口→预设参数→本地AI策略→语义/OCR/识别→策略决策→资源/输出
- 接口: POST `/api/workflow/run/basic3` | 步骤: 入口→参数→云AI策略→联动增强→复杂推理→本地AI→向量库→全功能联动→策略决策→参数生成→资源/输出
- 接口: POST `/api/workflow/override` | 步骤: 开关突破限制、审计
- 接口: POST `/api/workflow/run/advanced` | 步骤: 高级编排链路执行

**AI识别与训练(node.ai)**
- 接口: POST `/api/ai/archive-failures` | 步骤: 失败样本落盘、索引与加密
- 接口: POST `/api/ai/analyze-failures` | 步骤: pandas/numpy 分析、报表
- 接口: POST `/api/ai/train` | 步骤: 训练管线定义、资源配额
- 接口: POST `/api/ai/update` | 步骤: 热更新与版本标记
- 接口: POST `/api/ai/predict` | 步骤: 推理路由、超时与重试策略
- 接口: GET `/api/ai/service/health` | 步骤: API 健康探针
- 接口: POST `/api/ai/service/toggle` | 步骤: 服务启停、状态持久化
- 接口: GET `/api/ai/versions` | 步骤: 版本列表与活动标记
- 接口: GET `/api/ai/metrics` | 步骤: 指标汇总、Dashboard 对接
- 接口: POST `/api/ai/rollback` | 步骤: 版本选择、回滚
- 接口: POST `/api/ai/ensemble` | 步骤: 多模型融合策略
- 接口: POST `/api/ai/logs/archive` | 步骤: 日志归档与加密
- 接口: POST `/api/ai/evaluate-switch` | 步骤: 自动评估与版本切换
- 接口: POST `/api/ai/self-heal` | 步骤: 异常信号捕获与自修复
- 接口: POST `/api/ai/autogen/script` | 步骤: AutoGPT/LangChain 生成脚本

**交叉解析(node.xparse)**
- 接口: POST `/api/xparse/tasks` | 步骤: 创建任务、保存策略
- 接口: POST `/api/xparse/run/:id` | 步骤: 执行编排、收集结果
- 接口: GET `/api/xparse/status/:id` | 步骤: 状态监控、进度
- 接口: GET `/api/xparse/result/:id` | 步骤: 结果取回与缓存

**反追反穿(node.anti)**
- 接口: POST `/api/anti/trace/config` | 步骤: 策略配置与校验
- 接口: POST `/api/anti/bypass/toggle` | 步骤: 开关控制、审计
- 接口: POST `/api/anti/trace/run` | 步骤: 执行反追策略
- 接口: GET `/api/anti/trace/evaluate` | 步骤: 效果评估与报告

**极限突破(node.limit)**
- 接口: POST `/api/limit/override/config` | 步骤: 级别参数与策略落地
- 接口: POST `/api/limit/override/toggle` | 步骤: 总开关与日志
- 接口: POST `/api/limit/override/run` | 步骤: 场景模拟执行
- 接口: GET `/api/limit/override/report/:id` | 步骤: 风险报告生成

**破译挑战(node.challenge)**
- 接口: POST `/api/challenge/create` | 步骤: 类型与载荷校验、入库
- 接口: POST `/api/challenge/run/:id` | 步骤: 调度执行、记录产出
- 接口: GET `/api/challenge/status/:id` | 步骤: 状态与进度查询
- 接口: GET `/api/challenge/report/:id` | 步骤: 评测报告生成

**系统设置(node.system)**
- 接口: GET `/api/system/settings` | 步骤: 加载全局配置
- 接口: POST `/api/system/settings` | 步骤: 更新配置、审计
- 接口: GET `/api/system/health` | 步骤: 依赖健康聚合
- 接口: POST `/api/system/backup` | 步骤: 备份快照
- 接口: POST `/api/system/restore` | 步骤: 快照恢复

**权限管理(node.rbac)**
- 接口: GET `/api/rbac/users` | 步骤: 用户列表与分页
- 接口: GET `/api/rbac/roles` | 步骤: 角色列表
- 接口: GET `/api/rbac/policies` | 步骤: 策略列表
- 接口: POST `/api/rbac/bind-role` | 步骤: 用户角色绑定、审计
- 接口: POST `/api/rbac/bind-policy` | 步骤: 角色策略绑定、审计

**API 接口文档(node.docs)**
- 接口: GET `/api/docs/spec` | 步骤: 拉取规范与版本
- 接口: POST `/api/docs/sync` | 步骤: 同步更新规范
- 接口: GET `/api/docs/health` | 步骤: 文档生成健康检查

**后端控制(node.backend)**
- 接口: POST `/api/backend/toggle` | 步骤: 服务启停、权限校验
- 接口: GET `/api/backend/status` | 步骤: 服务状态聚合
- 接口: GET `/api/backend/logs` | 步骤: 日志拉取与过滤

**AI 训练(node.train)**
- 接口: POST `/api/train/tasks` | 步骤: 训练任务创建、数据集校验
- 接口: POST `/api/train/run/:id` | 步骤: 训练执行、资源管理
- 接口: GET `/api/train/status/:id` | 步骤: 状态轮询与里程碑
- 接口: GET `/api/train/eval/:id` | 步骤: 评估指标与报告

**FastAPI 服务联动(node.fastapi)**
- 接口: POST `/api/fastapi/register` | 步骤: 服务注册、路由验证
- 接口: POST `/api/fastapi/pipeline` | 步骤: 联动编排保存
- 接口: GET `/api/fastapi/health` | 步骤: 健康聚合与告警

**表格化对接清单（仅分析占位）**
状态标签说明：🟢 可用 / 🟡 部分完成 / 🔴 未部署

参数部署 (node.params)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 | 所属文档章节 | 状态 |
|---|---|---|---|---|---|---|
| 获取配置 | GET | /api/params/config | version, scope | 参数模型/版本表、鉴权、读取缓存 | 01/11 结构优化与入口 | 🔴 |
| 保存配置 | POST | /api/params/config | config, versioning | 校验入参、写入版本记录、触发下发事件 | 01/11 结构优化与入口 | 🔴 |
| 校验参数 | POST | /api/params/validate | concurrency, depth, timeout, proxy | 范围/连通性校验、生成诊断报告 | 05 Fuzz与参数变异 | 🔴 |
| 目标枚举 | GET | /api/params/targets | - | 返回公开/个人/政府公开、缓存策略 | 01 主窗口与分区 | 🔴 |
| 风控识别 | POST | /api/params/risk-check | keywords, strategies | 关键词/策略匹配、风险分级 | 02 风控与突破 | 🔴 |
| 排序预览 | POST | /api/params/rank-preview | weights, rules | 规则权重计算、示例预览 | 06 数据挖掘与分析 | 🔴 |

智能调度 (node.scheduler)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 | 所属文档章节 | 状态 |
|---|---|---|---|---|---|---|
| 启动调度 | POST | /api/scheduler/start | rateLimit, priority | 初始化队列/限流器/优先级策略 | 08 插件化与调度 | 🔴 |
| 停止调度 | POST | /api/scheduler/stop | - | 安全停机、保存队列状态 | 08 插件化与调度 | 🔴 |
| 状态查询 | GET | /api/scheduler/status | - | 进度汇总、线程/队列可视化 | 08 插件化与调度 | 🔴 |
| 注册AI服务 | POST | /api/ai/register | model, route | 服务目录登记、路由校验 | 04 AI模型与训练 | 🔴 |
| AI编排 | POST | /api/ai/pipeline | stages | 保存编排、输出 DAG | 08 插件化与调度 | 🔴 |

采集任务 (node.crawler)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 | 所属文档章节 | 状态 |
|---|---|---|---|---|---|---|
| 查询列表 | GET | /api/crawler/tasks | q, status, page, size | 列表/分页/过滤、索引优化 | 03 分布式采集与代理池 | 🔴 |
| 创建任务 | POST | /api/crawler/tasks | mode, payload | 模式选择、校验、入库 | 03 分布式采集与代理池 | 🔴 |
| 表单校验 | POST | /api/crawler/validate | fields | 字段校验、错误定位 | 03 分布式采集与代理池 | 🔴 |
| 任务详情 | GET | /api/crawler/tasks/:id | - | 明细查询、审计轨迹 | 03 分布式采集与代理池 | 🔴 |
| 启动任务 | POST | /api/crawler/tasks/:id/start | - | 启动消费、资源配额 | 03 分布式采集与代理池 | 🔴 |
| 停止任务 | POST | /api/crawler/tasks/:id/stop | - | 停止任务、释放资源 | 03 分布式采集与代理池 | 🔴 |

枚举任务 (node.enum)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 任务列表 | GET | /api/enum/tasks | - | 列表与状态聚合 |
| BIN校验 | POST | /api/enum/bin-check | bin | 规则校验、报告生成 |
| 游戏卡枚举 | POST | /api/enum/game-card | rules | 规则集执行、采样验证 |
| 虚拟卡枚举 | POST | /api/enum/virtual-card | rules | 规则加载、源管理 |
| 助记词枚举 | POST | /api/enum/mnemonic | alphabet, length | 词表生成、并发控制 |

识别任务 (node.recognize)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 | 所属文档章节 | 状态 |
|---|---|---|---|---|---|---|
| 提交识别 | POST | /api/recognize/submit | type, payload | 类型判定、存储、返回任务ID | 06 数据挖掘与分析 | 🔴 |
| 识别状态 | GET | /api/recognize/status/:id | - | 状态轮询、阶段结果 | 06 数据挖掘与分析 | 🔴 |
| 路由追踪 | GET | /api/recognize/trace/:id | - | 追踪与位置分析 | 06 数据挖掘与分析 | 🔴 |
| DNS/ASN | GET | /api/netinfo | domain, ip | 查询与缓存 | 06 数据挖掘与分析 | 🔴 |
| 社媒聚合 | GET | /api/social/aggregate | keyword | 来源抓取、聚合排序 | 06 数据挖掘与分析 | 🔴 |

破解任务 (node.crack)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 | 所属文档章节 | 状态 |
|---|---|---|---|---|---|---|
| 验证码破解 | POST | /api/crack/captcha | image | OCR/识别模型、置信度 | 02 风控与突破 | 🔴 |
| URL编解码 | POST | /api/crack/url | text, mode | 编解码、边界校验 | 02 风控与突破 | 🔴 |
| 网页解析 | POST | /api/crack/webparse | url | 页面解析、提取规则 | 02 风控与突破 | 🔴 |
| ZIP解锁 | POST | /api/crack/zip | file, pass | 解锁流程、安全限制 | 02 风控与突破 | 🔴 |
| 卡信息校验 | POST | /api/crack/card | number, date, cvv | 校验/风险边界 | 02 风控与突破 | 🔴 |

自动化状态 (node.auto)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 | 所属文档章节 | 状态 |
|---|---|---|---|---|
| 运行总况 | GET | /api/auto/overview | - | 模块心跳汇总 | 12 极限自动化与自愈 | 🔴 |
| 进度聚合 | GET | /api/auto/progress | - | 进度表与图表 | 12 极限自动化与自愈 | 🔴 |
| 安全级别 | GET | /api/auto/security-levels | - | 枚举与描述 | 14 合规安全与风控日志 | 🔴 |
| 关键词策略 | POST | /api/auto/keyword-strategy | level, keywords | 策略生成与保存 | 06 数据挖掘与分析 | 🔴 |

日志管理 (node.logs)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 部署日志 | GET | /api/logs/deploy | range | 拉取/分页 |
| 下载安装日志 | GET | /api/logs/install | range | 拉取/分页 |
| IP登录日志 | GET | /api/logs/ip-login | range | 聚合 |
| 使用日志 | GET | /api/logs/usage | range | 查询 |
| 清理日志 | POST | /api/logs/cleanup | range, types | 清理/备份策略 |

工作流部署 (node.workflow)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 部署/回滚 | POST | /api/workflow/deploy | version, action | 打包与发布/回滚 |
| 基础逻辑1 | POST | /api/workflow/run/basic1 | input | 编排执行 |
| 基础逻辑2 | POST | /api/workflow/run/basic2 | input | 编排执行 |
| 基础逻辑3 | POST | /api/workflow/run/basic3 | input | 编排执行 |
| 突破开关 | POST | /api/workflow/override | enabled | 开关与审计 |

AI识别与训练 (node.ai)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 失败样本归档 | POST | /api/ai/archive-failures | samples | 落盘/索引/加密 |
| 样本分析 | POST | /api/ai/analyze-failures | window, metrics | pandas/numpy 报表 |
| 模型训练 | POST | /api/ai/train | samples, params | 管线定义/资源配额 |
| 模型更新 | POST | /api/ai/update | params | 热更新/版本标记 |
| 识别预测 | POST | /api/ai/predict | data | 推理路由/超时重试 |
| 版本管理 | GET | /api/ai/versions | - | 列表与活动标记 |
| 指标监控 | GET | /api/ai/metrics | range | 指标汇总/Dashboard |

交叉解析 (node.xparse)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 创建任务 | POST | /api/xparse/tasks | sources, strategy | 保存策略与任务 |
| 执行任务 | POST | /api/xparse/run/:id | - | 编排执行、结果收集 |
| 状态查询 | GET | /api/xparse/status/:id | - | 状态与进度 |
| 结果获取 | GET | /api/xparse/result/:id | - | 结果与缓存 |

反追反穿 (node.anti)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 策略配置 | POST | /api/anti/trace/config | routes, mix | 配置与校验 |
| 反穿开关 | POST | /api/anti/bypass/toggle | enabled | 开关控制/审计 |
| 反追执行 | POST | /api/anti/trace/run | target | 执行策略 |
| 策略评估 | GET | /api/anti/trace/evaluate | - | 效果评估与报告 |

极限突破 (node.limit)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 突破配置 | POST | /api/limit/override/config | levels, params | 级别参数与策略 |
| 突破开关 | POST | /api/limit/override/toggle | enabled | 开关与日志 |
| 场景执行 | POST | /api/limit/override/run | scenario | 模拟执行 |
| 风险报告 | GET | /api/limit/override/report/:id | - | 报告生成 |

破译挑战 (node.challenge)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 创建挑战 | POST | /api/challenge/create | type, payload | 类型校验/入库 |
| 执行挑战 | POST | /api/challenge/run/:id | - | 调度执行/产出记录 |
| 挑战状态 | GET | /api/challenge/status/:id | - | 状态与进度 |
| 评测报告 | GET | /api/challenge/report/:id | - | 报告生成 |

系统设置 (node.system)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 获取设置 | GET | /api/system/settings | - | 加载全局配置 |
| 更新设置 | POST | /api/system/settings | settings | 更新与审计 |
| 系统健康 | GET | /api/system/health | - | 依赖健康聚合 |
| 备份 | POST | /api/system/backup | - | 快照生成 |
| 恢复 | POST | /api/system/restore | snapshot | 快照恢复 |

权限管理 (node.rbac)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 用户列表 | GET | /api/rbac/users | page | 分页/过滤 |
| 角色列表 | GET | /api/rbac/roles | - | 列表 |
| 策略列表 | GET | /api/rbac/policies | - | 列表 |
| 绑定角色 | POST | /api/rbac/bind-role | userId, roleId | 绑定与审计 |
| 绑定策略 | POST | /api/rbac/bind-policy | roleId, policyId | 绑定与审计 |

API 接口文档 (node.docs)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 拉取规范 | GET | /api/docs/spec | - | 拉取规范与版本 |
| 同步规范 | POST | /api/docs/sync | spec | 更新规范 |
| 文档健康 | GET | /api/docs/health | - | 生成健康检查 |

后端控制 (node.backend)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 服务启停 | POST | /api/backend/toggle | service, enabled | 权限校验/启停 |
| 服务状态 | GET | /api/backend/status | service | 状态聚合 |
| 后端日志 | GET | /api/backend/logs | service, range | 拉取/过滤 |

AI 训练 (node.train)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 创建训练 | POST | /api/train/tasks | dataset, params | 任务创建/校验 |
| 执行训练 | POST | /api/train/run/:id | - | 执行/资源管理 |
| 训练状态 | GET | /api/train/status/:id | - | 状态与里程碑 |
| 训练评估 | GET | /api/train/eval/:id | - | 指标与报告 |

FastAPI 服务联动 (node.fastapi)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 服务注册 | POST | /api/fastapi/register | name, url, routes | 注册与验证 |
| 联动编排 | POST | /api/fastapi/pipeline | stages | 保存编排 |
| 联动健康 | GET | /api/fastapi/health | - | 健康聚合与告警 |

安全策略 (node.security)

| 接口 | 方法 | 路径 | 关键参数 | 落地步骤 |
|---|---|---|---|---|
| 登录速率限制 | POST | /api/security/rate-limit | route, limit, window | 策略入库/反代配置 |
| 2FA 管理 | POST | /api/security/2fa | userId, method, enable | 2FA 开启与绑定 |
| WAF 状态 | GET | /api/security/waf/status | - | WAF/ModSecurity 状态聚合 |
| 上传扫描 | POST | /api/upload/scan | fileId | 扫描与日志对接 |
| API Key 创建 | POST | /api/keys/create | name, quota | 生成与配额 |
| API Key 撤销 | POST | /api/keys/revoke | key | 撤销与审计 |
| API Key 列表 | GET | /api/keys/list | - | 列表与过滤 |
| IP 限速策略 | POST | /api/security/ip-throttle | ip, limit, window | 入库与反代配置 |

# 统一接口映射表（镜像副本）
# 统一接口映射表 · 项目核心说明（不写死，模块化）

本文件为前后端的统一真源，描述项目核心结构、七大页面与其运行逻辑映射、统一启动项、以及 API 文档页面的卡片规范。所有页面的功能逻辑必须来自 JS/CSS 模块，而不是写死在 HTML 中。

## 一、统一启动项（环境可配置）
- 版本指纹：页面使用 `?v=__ASSET_VERSION__`，由服务端在运行时注入。
示例：
```
ASSET_VERSION=1.2.3 \
API_PROTO=https API_HOST=backend.local API_PORT=9000 \
node dev-server.js
```

## 二、HTML 的职责边界（禁止写死业务逻辑）
- HTML 只负责：容器结构、主题框架、插槽、挂载点。
- 统一所有页面运行逻辑来自：
  - `/static/js/modules/*.js`
  - `/static/css/modules/*.css`
- 本文件（统一接口映射表.md）中每个节点内容作为独立模块的说明源。

## 三、七大页面（仅定义 UI 主题，逻辑由模块驱动）
以下所有网页独立呈现，页面呈现的功能都必须根据联动 JS/CSS 产生部署；HTML 中不写死功能逻辑，只保留必要挂载点。

1. `index.html`（首页）
	- 交互点：顶部导航、侧边分组菜单点击触发对应模块渲染器；按钮发起 `fetch` 请求，响应数据显示在卡片下的 `<pre>`；仪表盘 KPI 动态刷新。
	- 依赖：`/static/js/pages/index.js` 调用 `/static/js/modules/index.js` 及组件库。

2. `monitor.html`（后端监控）
	- 典型逻辑：引入 `ws.js` 或 `monitor.js`（目录含 `static/js/monitor.js`），使用 `WS.connectPipelineWS(taskId)` 订阅任务进度/日志流，渲染进度条、图表（`components/charting.js`、`progress.js`、`spinner.js`、`toast.js`）。
	- 依赖：`/static/js/pages/monitor.js` + 组件库。

3. `run.html`（运行脚本）
	- 表单提交事件：`run.js` 监听 `#runForm` 提交，取 `name` 与 `params`（JSON），调用 `POST /api/run`，把文本结果写入 `#result`。
	- 依赖：`/static/js/pages/run.js` + 表单/进度/提示组件。

4. `api-doc.html`（前后端接口统一页面）
	- 逻辑：静态说明或由 `modules.js` 拉取模块列表，展示接口路径、示例请求/响应；呈现统一的卡片结构（见“卡片规范”）。
	- 依赖：`/static/js/pages/api-doc.js` + 组件库。

5. `ai-demo.html`（AI 调度）
	- 逻辑：统一从模块注册中心加载 AI 相关任务与策略，动态渲染任务进度与结果。
	- 依赖：`/static/js/pages/ai-demo.js` + 模块/组件。

6. `login.html`（登录页面，预留）
	- 逻辑：依赖 `static/js/auth.js` 提供统一鉴权头与登录态；页面内只放表单挂载点。
	- 依赖：`/static/js/pages/login.js` + `auth.js`。

7. `rbac.html`（权限矩阵映射表）
	- 逻辑：`data-feature` 属性控制 UI 权限可见性（`Permission.applyGuards()`）；从模块源加载角色/权限配置矩阵。
	- 依赖：`/static/js/pages/rbac.js` + `auth.js`。

8. `DAG.html`（企业 DAG 可视化流水线）
	- 逻辑：结合 `ws.js` 与 `components/charting.js` 展示任务/流水线拓扑与实时状态；页面初始可隐藏，DOMContentLoaded 后显示避免闪烁。
	- 依赖：`/static/js/pages/visual_pipeline.js` + 组件库。

9. `api-map.html`（统一接口映射表.md 的页面镜像）
	- 逻辑：以“统一真源”为主，页面镜像用于查看与提议同步；支持源→页面镜像与双向同步提议。

## 四、模块目录与约定
- JS 模块：`/static/js/modules/*.js`
- CSS 模块：`/static/css/modules/*.css`
- 组件库：`/static/js/components/*.js`、`/static/css/components/*.css`
- 页面入口：`/static/js/pages/*.js` 仅负责读取挂载点与调用模块渲染器。
- 权限与鉴权：`/static/js/auth.js` 提供统一鉴权头；`Permission.applyGuards()` 基于 `data-feature` 控制可见性。
- WebSocket：`/static/js/ws.js` 提供 `WS.connectPipelineWS(taskId)` 与其它订阅方法。

## 五、API 文档页面（api-doc.html）卡片规范（必须）
每一个接口或功能模块卡片，必须显示以下内容，用于监控前后端接口节点对齐：

1. 冒泡检测（事件/错误可冒泡至页面级处理器）
2. 定义的前端功能名称（模块名）
3. 一句话自然语言功能说明
4. 输入参数区域（动态生成，含类型/示例/必填）
5. 后端预留节点（对应 API/WS 路径或服务名）
6. 优化建议（性能/缓存/重试/安全）
7. 功能标签（AI / API / 脚本 / DAG）
8. 状态标识：
	- 🟢 可用
	- 🟡 部分完成
	- 🔴 未部署

卡片结构示例（伪结构）：
```
Card {
  name: "模块/接口名称",
  description: "一句话说明",
  tags: ["API", "DAG"],
  status: "green|yellow|red",
  inputs: [ { key, type, required, example } ],
  backend: { api: "/api/foo", ws: "/ws/foo" },
  suggestions: ["启用缓存", "指数退避重试"],
  events: { bubble: true }
}
```

## 六、页面与模块映射（样例）
- index 模块组：导航/菜单 → `modules/index.js` 渲染；KPI → `modules/metrics.js`；请求响应展示 → `components/card.js` + `<pre>`。
- monitor 模块组：WS 订阅 → `ws.js`；图表 → `components/charting.js`；进度 → `components/progress.js`；提示 → `components/toast.js`。
- run 模块组：表单处理 → `modules/run.js`；提交 → `POST /api/run`；结果写入 `#result`。
- api-doc 模块组：模块清单 → `modules/registry.js`；卡片渲染 → `modules/api-doc.js`；样式 → `css/components/*.css`。
- ai-demo 模块组：AI 调度 → `modules/ai-demo.js`；进度渲染 → `components/progress.js`。
- login/rbac 模块组：鉴权 → `auth.js`；权限卫士 → `Permission.applyGuards()`；数据源 → `modules/rbac.js`。
- DAG 模块组：拓扑渲染 → `modules/visual_pipeline.js`；WS → `ws.js`；图表 → `components/charting.js`。

## 七、统一约束与质量保证
- 不在 HTML 中写死任何业务逻辑；入口 JS 只做挂载和调度。
- 模块必须声明依赖、输入参数与后端节点，并在 `api-doc.html` 卡片中可见。
- 所有网络交互必须通过统一的 `fetch`/WS 封装层（包含鉴权头与错误冒泡）。
- 版本号、后端地址等不可写死，均通过环境或注入实现。

## 八、监控与对齐
- 前端卡片状态用于追踪部署进度，后端接口与 WS 节点需在 `api-map.html` 与 `api-doc.html` 对齐呈现。
- 建议在 CI 中生成模块索引并校验页面引用一致性。

---
如需新增页面或模块，请遵循以上约定，并在此文档补充模块说明（名称、描述、输入、后端节点、标签、状态、优化建议）。
